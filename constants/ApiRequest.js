//authenticated
const Product = {
    URL_RESTFUL: '/products'
}

const Auth = {
    REGISTER: '/user/register',
    LOGIN: '/user/login',
    REFRESH: '/user/refresh'
}

module.exports = {
    Product,
    Auth
};
