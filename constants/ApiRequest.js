//authenticated
const Product = {
    URL_RESTFUL: '/products'
}

const Auth = {
    REGISTER: '/user/register',
    LOGIN: '/user/login',
    REFRESH: '/user/refresh',
    CURRENT_USER: '/user/me'
}

module.exports = {
    Product,
    Auth
};
